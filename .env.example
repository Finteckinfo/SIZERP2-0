# Backend API URL
# For local development: http://localhost:3000
# For production: https://your-backend-domain.com
VITE_BACKEND_URL=http://localhost:3000

# WalletConnect Project ID
# Get from https://dashboard.reown.com/
# IMPORTANT: Should match across all frontend projects
VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# NextAuth SSO Configuration
# The ERP (erp.siz.land) uses NextAuth sessions from the main site (siz.land)
# Authentication is centralized on siz.land and shared via SSO cookies
# No additional auth configuration needed in the ERP
# Sessions are validated by calling the backend /api/auth/session endpoint
